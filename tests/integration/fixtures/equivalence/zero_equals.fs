\ 0= equivalence — ( n -- flag ): 1 if top == 0, else 0.
\ Exercises both arms: a non-zero input (5 → 0) and a zero input (0 → 1).
\ REPL events (via `.`) must match the compiled-then-interpreted events.
5 0= .
0 0= .
